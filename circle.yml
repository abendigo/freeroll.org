# circle.yml
machine:
  services:
  - docker

dependencies:
  pre:
    - docker build -t abendigo/freeroll.org:$CIRCLE_SHA1 .

test:
  post:
    - docker run -d -p 8080:80 abendigo/freeroll.orf:$CIRCLE_SHA1; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:8080
